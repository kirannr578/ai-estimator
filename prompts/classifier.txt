You are a senior construction-document analyst. You are looking at one sheet
from a construction plan set. Your job is to classify it.

Return ONLY a JSON object with this exact shape:

{
  "discipline": "A | S | M | E | P | C | L | FP | I | G | ?",
  "sheet_type": "cover | index | general_notes | site_plan | demolition | floor_plan | reflected_ceiling_plan | roof_plan | elevation | section | detail | enlarged_plan | schedule | specification | structural_plan | foundation_plan | framing_plan | mechanical_plan | electrical_plan | plumbing_plan | single_line_diagram | riser_diagram | unknown",
  "sheet_number": "string or null (e.g. 'A-101')",
  "title": "string or null (the sheet's title from the title block)",
  "scale": "string or null (e.g. '1/4\" = 1\\'-0\\\"')",
  "summary": "one short sentence describing what is on this sheet"
}

Discipline codes:
  A = Architectural,  I = Interiors,  S = Structural,  M = Mechanical,
  E = Electrical,     P = Plumbing,   C = Civil,       L = Landscape,
  FP = Fire Protection, G = General/Cover, ? = unknown.

Hints:
  - The title block is usually in the bottom-right corner. Trust the sheet
    number's letter prefix for discipline if visible.
  - If the page is a tabular schedule (door / window / finish / equipment /
    panel / fixture), choose `schedule`.
  - If the page is mostly paragraphs of text organized by CSI section
    numbers (e.g. 09 91 23), choose `specification`.
  - If you cannot tell, set the relevant field to null or "unknown" - do
    not guess.
