# Core
streamlit>=1.40.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# PDF processing
pymupdf>=1.24.10          # fitz: text, vector, raster extraction
Pillow>=10.4.0

# LLM providers (install whichever you use; both are listed for convenience)
anthropic>=0.39.0
openai>=1.54.0

# Data + export
pandas>=2.2.3
openpyxl>=3.1.5
reportlab>=4.0,<5.0       # client-quote PDF builder

# Utilities
tenacity>=9.0.0           # retries for LLM calls
rapidfuzz>=3.10.0         # fuzzy matching for room/spec dedupe

# CWICR open cost-database matcher (F1)
# NOTE: the F1 spec listed `datasets>=2.18,<4.0` for HF dataset access;
# the actual CWICR dataset (datadrivenconstruction/OpenConstructionEstimate-
# DDC-CWICR) is distributed via GitHub, not Hugging Face, so we use a direct
# parquet download + pyarrow + pandas instead. `datasets` is therefore not
# pulled in.
scikit-learn>=1.4,<2.0           # TF-IDF first-pass over CWICR descriptions
sentence-transformers>=2.7,<4.0  # all-MiniLM-L6-v2 semantic re-rank (CPU)
pyarrow>=15.0                    # parquet reader for the CWICR dataset

# Proposal renderer (core/proposal_renderer/) — bid `proposal/*.md` to
# submission-quality PDF + pitch-deck PPTX. Pure-Python, Windows-friendly,
# no system deps (no Cairo / Pango / Chromium).
markdown>=3.6                    # Markdown -> HTML; tables / fenced_code / attr_list extensions
xhtml2pdf>=0.2.16                # HTML -> PDF (used for proposal-full.pdf)
python-pptx>=1.0                 # PPTX builder (pitch-deck + full styles)
pypdf>=4.0                       # used in renderer tests for round-trip text extraction

# Tier 1 pricing-data integrations (core/pricing/sources/, scripts/refresh_pricing.py)
# httpx is the HTTP client used by every public-source adapter (BLS / FRED /
# EIA / Davis-Bacon). Pinning a baseline of 0.28+ so `httpx.MockTransport` is
# available for offline unit tests (we use it instead of a separate
# `respx` / `pytest-httpx` dependency).
httpx>=0.28,<1.0
# python-dotenv is already declared above (used to load BLS/FRED/EIA API keys
# from .env into scripts/refresh_pricing.py).
